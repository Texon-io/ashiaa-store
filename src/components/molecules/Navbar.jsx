function Navbar() {
    return (
        <nav className={`text-right px-8 py-3 flex justify-between flex-row-reverse`}>
            <div className={`nav-logo font-reqaa text-2xl text-accent-dark`}>متجر بائعة الكتب</div>
            <div className={`nav-`}>s</div>
            <div className={`nav- `}>s</div>
        </nav>
    )
}

export default Navbar;
